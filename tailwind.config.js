/** @type {import('tailwindcss').Config} */
module.exports = {
    // NOTE: Configure to scan all files in src
    content: ["./src/**/*.{js,jsx,ts,tsx}"],
    theme: {
        extend: {},
    },
    plugins: [],
}
